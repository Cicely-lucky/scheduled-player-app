<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络访问（打开网址 / 网络音频流） -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 后台任务保持唤醒 -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- 开机后恢复定时任务 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Android 13+ 通知权限（定时触发提醒） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 前台服务播放媒体 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- 精确闹钟：Android 12+ 免授权直接授予（仅闹钟/日历类 App 可声明） -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- Android 12+ 精确闹钟（兜底：部分 ROM 走此权限） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- 全屏通知（Android 14+ 弹全屏提醒需要） -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>

    <application
        android:label="定时播放"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- 精确闹钟（android_alarm_manager_plus）-->
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false"/>
        <!-- 关键：闹钟触发后由 AlarmService（JobService）启动 Flutter 后台引擎执行回调。
             官方文档要求在 App 清单声明；此前缺失导致每次闹钟触发即崩溃：
             "Tried to schedule job for non-existent component: .../g4.a" -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false"/>
        <!-- 重启后恢复闹钟 -->
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- 后台自动播放：闹钟回调（Dart）发显式广播 → Receiver 拉起前台服务直接开播，
             无需点击通知（亮屏使用中/锁屏/熄屏均生效） -->
        <receiver
            android:name=".PlaybackReceiver"
            android:exported="false"/>
        <service
            android:name=".PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
</manifest>
