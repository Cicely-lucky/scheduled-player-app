<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络访问（打开网址 / 网络音频流） -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 后台任务保持唤醒 -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- 开机后恢复定时任务 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Android 13+ 通知权限（定时触发提醒） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 前台服务播放媒体 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- 精确闹钟：Android 12+ 免授权直接授予（仅闹钟/日历类 App 可声明） -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- Android 12+ 精确闹钟（兜底：部分 ROM 走此权限） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- 全屏通知（Android 14+ 弹全屏提醒需要） -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <!-- 悬浮窗权限：拥有 SYSTEM_ALERT_WINDOW 的 App 豁免后台启动 Activity（BAL），
         这是 Android 官方豁免列表明文条款。16:25 实测：前台服务状态本身不豁免
         （callingUidProcState=FOREGROUND_SERVICE 仍 BAL_BLOCK code=102，
         callingUidHasVisibleActivity=false 是关键短板）。授权方式：设置里开启
         「显示悬浮窗」或 adb appops set pkg SYSTEM_ALERT_WINDOW allow。 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- 包可见性（Android 11+）：不声明则 resolveActivity 看不到第三方 App——
         17:25 实测 bilibili:// 深链 resolver=null（adb shell 可解析是因为有
         QUERY_ALL_PACKAGES），导致回退到浏览器。声明后才能命中 tv.danmaku.bili -->
    <queries>
        <!-- B 站深链 -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="bilibili"/>
        </intent>
        <!-- B 站网页链接 -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https" android:host="www.bilibili.com"/>
        </intent>
        <!-- B 站短链 -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https" android:host="b23.tv"/>
        </intent>
        <!-- 通用 http/https（浏览器兜底） -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
    </queries>

    <application
        android:label="定时播放"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- 精确闹钟（android_alarm_manager_plus）-->
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false"/>
        <!-- 关键：闹钟触发后由 AlarmService（JobService）启动 Flutter 后台引擎执行回调。
             官方文档要求在 App 清单声明；此前缺失导致每次闹钟触发即崩溃：
             "Tried to schedule job for non-existent component: .../g4.a" -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false"/>
        <!-- 重启后恢复闹钟 -->
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- 后台自动播放：闹钟回调（Dart）发显式广播 → Receiver 拉起前台服务直接开播，
             无需点击通知（亮屏使用中/锁屏/熄屏均生效） -->
        <receiver
            android:name=".PlaybackReceiver"
            android:exported="false"/>
        <service
            android:name=".PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"/>
        <!-- 网址任务自动打开的前台服务桥：闹钟/定时器到点 → 广播 →
             拉起本服务转前台 → 拉起 UrlBridgeActivity（锁屏可见）打开目标网址 →
             数秒后自动收摊 -->
        <service
            android:name=".UrlBridgeService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"/>

        <!-- 锁屏可见的透明中转 Activity：由 UrlBridgeService 拉起（BAL 由悬浮窗
             豁免放行），showWhenLocked+turnScreenOn 让它盖在锁屏之上（系统闹钟
             同款机制），再由它打开目标 App——绕过 MIUI KeyguardLocked 拦截。
             17:25 实测：服务直接 startActivity 目标 App 虽过 BAL 但被
             「Permission Denied Activity KeyguardLocked」拦下（result code=102）。 -->
        <activity
            android:name=".UrlBridgeActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
</manifest>
